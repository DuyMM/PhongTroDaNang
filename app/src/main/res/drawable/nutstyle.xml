<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- pressed -->
    <!-- focused -->
    <!-- default -->
    <item android:drawable="@color/colorButtonForce" android:state_pressed="true" />
    <item android:drawable="@color/colorButtonForce" android:state_focused="true" />
    <item android:drawable="@color/colorButton" />
</selector>